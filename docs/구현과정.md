# macintosh os

## 프로젝트 설정

### Vite 설정

- Vite 설치 명령어 : `npm create vite@latest`
- Vite 선정 이유
  - React 프로젝트 설정 방식에는 `Vite`와 `CRA`(`Create React App`) 방식이 존재했다 하지만 CRA 방식은 최근 사용하지 않는다고 하며 Vite를 사용하여 빠르게 웹 개발 시작이 가능하다고 하여 Vite로 선정하였다.
  - `Vite` : 프론트엔드 빌드 도구
