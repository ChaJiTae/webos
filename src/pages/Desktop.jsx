const Desktop = () => {
  return (
    <div
      className="fixed inset-0 overflow-hidden bg-center bg-cover "
      style={{
        backgroundImage: `linear-gradient(to bottom,#A8E6CF,#6DD5ED)`,
      }}
    ></div>
  );
};

export default Desktop;
